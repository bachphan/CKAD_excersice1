apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Lưu ý: 02-secret.yaml (giá trị thật) KHÔNG liệt kê ở đây — secret được tạo/apply riêng bằng tay
# (xem README), không đi qua kustomize, vì file thật bị gitignore, chỉ có 02-secret.yaml.example.
resources:
  - 00-namespace.yaml
  - 01-configmap.yaml
  - 10-product-service.yaml
  - 11-user-service.yaml
  - 12-order-service.yaml
  - 13-frontend.yaml
  - 20-networkpolicy.yaml
  - 21-networkpolicy-egress.yaml
  - 30-pv-pvc.yaml
  - 40-hpa.yaml
  - 50-postgres.yaml
  - 60-stock-monitor.yaml
  - 70-resourcequota.yaml
