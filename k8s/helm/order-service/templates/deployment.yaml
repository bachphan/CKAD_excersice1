apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
        tier: backend
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      initContainers:
      - name: init-config
        image: {{ .Values.images.busybox }}
        imagePullPolicy: IfNotPresent
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
        command: ["sh", "-c"]
        args:
          - |
            echo "== order-service effective config ==" > /init-config/summary.txt
            echo "PGHOST=$PGHOST" >> /init-config/summary.txt
            echo "PGPORT=$PGPORT" >> /init-config/summary.txt
            echo "PGDATABASE=babymilk_orders" >> /init-config/summary.txt
            cat /init-config/summary.txt
        env:
        - name: PGHOST
          valueFrom:
            configMapKeyRef:
              name: babymilk-config
              key: PGHOST
        - name: PGPORT
          valueFrom:
            configMapKeyRef:
              name: babymilk-config
              key: PGPORT
        resources:
          requests:
            cpu: 10m
            memory: 16Mi
          limits:
            cpu: 50m
            memory: 32Mi
        volumeMounts:
        - name: init-config-data
          mountPath: /init-config
      containers:
      - name: order-service
        image: {{ .Values.image }}
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
        ports:
        - containerPort: 4103
        volumeMounts:
        - name: init-config-data
          mountPath: /init-config
          readOnly: true
        env:
        - name: PORT
          value: "4103"
        - name: PGHOST
          valueFrom:
            configMapKeyRef:
              name: babymilk-config
              key: PGHOST
        - name: PGPORT
          valueFrom:
            configMapKeyRef:
              name: babymilk-config
              key: PGPORT
        - name: PGUSER
          valueFrom:
            configMapKeyRef:
              name: babymilk-config
              key: PGUSER
        - name: PGDATABASE
          value: "babymilk_orders"
        - name: PGPASSWORD
          valueFrom:
            secretKeyRef:
              name: babymilk-secret
              key: POSTGRES_PASSWORD
        - name: PRODUCT_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: babymilk-config
              key: PRODUCT_SERVICE_URL
        - name: PRODUCT_SERVICE_TIMEOUT_MS
          valueFrom:
            configMapKeyRef:
              name: babymilk-config
              key: PRODUCT_SERVICE_TIMEOUT_MS
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: babymilk-secret
              key: JWT_SECRET
        - name: INTERNAL_API_KEY
          valueFrom:
            secretKeyRef:
              name: babymilk-secret
              key: INTERNAL_API_KEY
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 150m
            memory: 256Mi
        readinessProbe:
          httpGet:
            path: /healthz
            port: 4103
          initialDelaySeconds: 3
          periodSeconds: 5
          timeoutSeconds: 3
        livenessProbe:
          httpGet:
            path: /healthz
            port: 4103
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
      - name: log-shipper
        image: {{ .Values.images.busybox }}
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsNonRoot: false
          runAsUser: 0
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        command: ["sh", "-c"]
        args:
          - |
            until ls /var/log/containers/${POD_NAME}_{{ .Values.namespace }}_order-service-*.log 1>/dev/null 2>&1; do sleep 2; done
            exec tail -F /var/log/containers/${POD_NAME}_{{ .Values.namespace }}_order-service-*.log
        volumeMounts:
        - name: varlog
          mountPath: /var/log
          readOnly: true
        resources:
          requests:
            cpu: 10m
            memory: 16Mi
          limits:
            cpu: 50m
            memory: 32Mi
      - name: ambassador-nginx
        image: {{ .Values.images.nginx }}
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsNonRoot: false
          runAsUser: 0
          allowPrivilegeEscalation: false
        ports:
        - containerPort: 4003
        volumeMounts:
        - name: ambassador-conf
          mountPath: /etc/nginx/conf.d/default.conf
          subPath: order-service.conf
        resources:
          requests:
            cpu: 20m
            memory: 16Mi
          limits:
            cpu: 100m
            memory: 64Mi
      volumes:
      - name: init-config-data
        emptyDir: {}
      - name: varlog
        hostPath:
          path: /var/log
      - name: ambassador-conf
        configMap:
          name: ambassador-nginx-conf
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: order-service
  ports:
  - port: 4003
    targetPort: 4003
