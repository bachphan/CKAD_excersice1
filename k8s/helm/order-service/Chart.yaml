apiVersion: v2
name: order-service
description: >
  order-service của babymilk-shop — cài ĐỘC LẬP được, nhưng yêu cầu chart babymilk-infra
  đã cài trước vào CÙNG namespace.
type: application
version: 1.0.0
appVersion: "2.1"
